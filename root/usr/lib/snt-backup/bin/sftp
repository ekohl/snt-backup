#!/usr/bin/perl -w

use strict;

my $rmt_id_dsa  = $ENV{'HOME'}.'/.ssh/id_dsa';
$rmt_id_dsa     = $ENV{'IDENTITY'} if defined $ENV{'IDENTITY'};

exec ('/usr/bin/sftp', '-o', "IdentityFile $rmt_id_dsa", @ARGV);
